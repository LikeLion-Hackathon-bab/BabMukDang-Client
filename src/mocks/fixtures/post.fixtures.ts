/** @fileoverview Article-compatible post mock fixtures. */
import { articleFixtures } from './article.fixtures'

export const mockPostResponses = articleFixtures.homeList.items
export const mockSinglePostResponse = mockPostResponses[0]

/** @deprecated Recruit mock is removed. Use MealPlan or Article fixtures. */
